{% extends 'base.html.twig' %}

{% block title %}ExamPassingScore{% endblock %}

{% block body %}
    <h1>ExamPassingScore</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exam_passing_score.id }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ exam_passing_score.score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exam_passing_score_index') }}">back to list</a>

    <a href="{{ path('exam_passing_score_edit', {'id': exam_passing_score.id}) }}">edit</a>

    {{ include('exam_passing_score/_delete_form.html.twig') }}
{% endblock %}
