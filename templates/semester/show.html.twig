{% extends 'base.html.twig' %}

{% block title %}Semester{% endblock %}

{% block body %}
    <h1>Semester</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ semester.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ semester.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ semester.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('semester_index') }}">back to list</a>

    <a href="{{ path('semester_edit', {'id': semester.id}) }}">edit</a>

    {{ include('semester/_delete_form.html.twig') }}
{% endblock %}
