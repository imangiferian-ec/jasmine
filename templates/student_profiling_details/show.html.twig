{% extends 'base.html.twig' %}

{% block title %}StudentProfilingDetails{% endblock %}

{% block body %}
    <h1>StudentProfilingDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student_profiling_detail.id }}</td>
            </tr>
            <tr>
                <th>DateProfiled</th>
                <td>{{ student_profiling_detail.dateProfiled ? student_profiling_detail.dateProfiled|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Remarks</th>
                <td>{{ student_profiling_detail.remarks }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ student_profiling_detail.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('student_profiling_details_index') }}">back to list</a>

    <a href="{{ path('student_profiling_details_edit', {'id': student_profiling_detail.id}) }}">edit</a>

    {{ include('student_profiling_details/_delete_form.html.twig') }}
{% endblock %}
