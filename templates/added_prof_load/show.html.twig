{% extends 'base.html.twig' %}

{% block title %}AddedProfLoad{% endblock %}

{% block body %}
    <h1>AddedProfLoad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ added_prof_load.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('added_prof_load_index') }}">back to list</a>

    <a href="{{ path('added_prof_load_edit', {'id': added_prof_load.id}) }}">edit</a>

    {{ include('added_prof_load/_delete_form.html.twig') }}
{% endblock %}
