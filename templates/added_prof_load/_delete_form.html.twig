<form method="post" action="{{ path('added_prof_load_delete', {'id': added_prof_load.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ added_prof_load.id) }}">
    <button class="btn">Delete</button>
</form>
