<form method="post" action="{{ path('curriculum_subject_delete', {'id': curriculum_subject.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ curriculum_subject.id) }}">
    <button class="btn">Delete</button>
</form>
