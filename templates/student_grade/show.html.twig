{% extends 'base.html.twig' %}

{% block title %}StudentGrade{% endblock %}

{% block body %}
    <h1>StudentGrade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student_grade.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('student_grade_index') }}">back to list</a>

    <a href="{{ path('student_grade_edit', {'id': student_grade.id}) }}">edit</a>

    {{ include('student_grade/_delete_form.html.twig') }}
{% endblock %}
