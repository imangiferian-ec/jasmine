{% extends 'base.html.twig' %}

{% block title %}Edit StudentGrade{% endblock %}

{% block body %}
    <h1>Edit StudentGrade</h1>

    {{ include('student_grade/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('student_grade_index') }}">back to list</a>

    {{ include('student_grade/_delete_form.html.twig') }}
{% endblock %}
