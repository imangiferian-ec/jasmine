<form method="post" action="{{ path('student_grade_delete', {'id': student_grade.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ student_grade.id) }}">
    <button class="btn">Delete</button>
</form>
